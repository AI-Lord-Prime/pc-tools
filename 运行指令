# 常用运行指令

## 开发启动（前端 + Tauri 桌面端）

```bash
npm run dev
```

## 纯前端开发（不启动 Tauri 窗口）

```bash
npm run vite
```

## 构建生产包

```bash
npm run build
npm run tauri build
```

若打包时反复下载 `nsis-3.zip`（GitHub 很慢），先预拉本地缓存：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/prefetch-nsis.ps1
```

缓存目录：`%LOCALAPPDATA%\tauri\NSIS`。缺 `nsis_tauri_utils.dll` 时 Tauri 会删掉整个 NSIS 再重下。

## 技术栈

- 前端：React 18 + TypeScript + Vite + shadcn/ui + Tailwind CSS
- 桌面：Tauri 1.x

## 其他

- `npm run preview`：预览生产构建
- `npm run tauri -- <命令>`：执行任意 Tauri CLI 命令
